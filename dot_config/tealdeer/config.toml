# Managed by chezmoi — edit the source, not this file:
#   chezmoi edit ~/.config/tealdeer/config.toml

[updates]
# Fetch the page cache on first run and refresh it when it's stale,
# instead of failing with "cache not found, run tldr --update" on a fresh
# host.
auto_update = true
auto_update_interval_hours = 720
