# Managed by dotfiles (features.ssh_agent). Read by the
# systemd user manager at login, so every user service — under niri that
# is the compositor itself and with it every app it launches — sees the
# socket of the ssh-agent.socket unit. Takes effect at the next login.
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/ssh-agent.socket
