# Managed by dotfiles. Read by the systemd user manager at login, so the
# compositor and everything it launches (launcher, key binds, xdg-open
# handlers) find ~/.local/bin the way an interactive shell does through
# .zshrc. Takes effect at the next login.
PATH=$HOME/.local/bin:$PATH
