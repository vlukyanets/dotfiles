Theme=FluentDark-solid
Font=Sans 14
MenuFont=Sans 14
TrayFont=Sans 16
# Render every input method (keyboard layouts and pinyin alike) as a
# short text label in the tray instead of a generic/engine icon, so the
# actually-active language is always legible at a glance
PreferTextIcon=True
ShowLayoutNameInIcon=True
