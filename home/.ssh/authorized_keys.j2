# Managed by dotfiles — edit ssh.authorized_keys in hosts/ or profiles/
# (the names) and data/ssh-pubkeys-collection.toml (the keys), not this file.
{% for name in ssh.authorized_keys %}
{{ ssh_keys[name] }} {{ name }}
{% endfor %}
