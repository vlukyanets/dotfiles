#!/bin/sh

set -eu

SUDO_CMD="${SUDO_CMD:-sudo}"

{{- if not .terminal.kitty.enabled }}

echo "==> Skipping kitty install (terminal.kitty.enabled is false for this host)"
exit 0
{{- end }}

echo "==> Installing kitty"
$SUDO_CMD pacman -S --needed --noconfirm kitty
