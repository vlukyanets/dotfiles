#!/bin/sh

set -eu

SUDO_CMD="${SUDO_CMD:-sudo}"

{{- if not .fonts.enabled }}

echo "==> Skipping font install (fonts.enabled is empty for this host)"
exit 0
{{- end }}

echo "==> Installing fonts"
$SUDO_CMD pacman -S --needed --noconfirm {{ join " " .fonts.enabled }}
