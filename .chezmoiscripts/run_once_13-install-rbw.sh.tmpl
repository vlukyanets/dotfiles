#!/usr/bin/env bash
{{ if not .rbw_enabled -}}
echo "==> rbw installation skipped (disabled in config)."
exit 0
{{- end }}

set -euo pipefail

echo "==> Installing rbw (Bitwarden CLI)..."

sudo pacman -S --needed --noconfirm \
    rbw \
    pinentry

echo "==> rbw installed."
